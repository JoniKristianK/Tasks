# Tasks
*in progress*

Tasks with Angular & TypeScript

* Clone project
* cd to the project folder (package.json)
* npm install
* npm run server
* new terminal
* ng serve
* app will run on localhost:4200
